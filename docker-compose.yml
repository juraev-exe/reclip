services:
  reclip:
    build: .
    image: reclip-local
    container_name: reclip
    ports:
      - "8899:8899"
    environment:
      HOST: 0.0.0.0
      PORT: 8899
    volumes:
      - ./downloads:/app/downloads
    restart: unless-stopped
